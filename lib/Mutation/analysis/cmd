# Can run this from top dir, which contain inputs in directory 'inputs'

set -u 

# Assuming that klee zesti is already on the path
PATH=$PATH:/media/thierry/TestMutants/KLEE-MART/klee-semu/build/bin/
intopdir=`pwd`/inputs
outtopdir=`pwd`

ScriptDir=$(dirname $0)

python $ScriptDir/run.py $outtopdir --exepath $intopdir/tests/program --runtest $intopdir/runtests.sh --testlist $intopdir/testscases.txt --martout $intopdir/martout --matrix $intopdir/SM.dat
