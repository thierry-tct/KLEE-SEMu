#! /bin/bash

# Do: 'source <This file>' in the the cmd script file

SEMU_CFG_ZESTI_EXE_DIR="$HOME/mytools/shadow/klee-change/Release+Asserts/bin/" #$(dirname `which klee`)
SEMU_CFG_SEMU_EXE_DIR=$(readlink -f ~/)"/mytools/klee-semu/build/Release+Asserts/bin" # "$SEMU_BINARY_DIR"
SEMU_CFG_LLVMGCC_EXE_DIR="$HOME/mytools/shadow/kleeDeploy/llvm-gcc4.2-2.9-x86_64-linux/bin" #$(dirname `which llvm-gcc`)
SEMU_CFG_LLVM27_EXE_DIR="$HOME/mytools/shadow/kleeDeploy/llvm-2.9/Release+Asserts/bin" #$(dirname `which llvm-link`)
